# LGM Prices: AWS Lambda

Deploy AWS Lambda function integrated with AWS API Gateway to request LGM card inventory on-demand.

## Usage:

```bash
curl -X GET 'https://URL/STAGE/price?card=Reanimate'
```
